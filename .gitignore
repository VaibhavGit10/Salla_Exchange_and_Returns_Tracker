# Dependencies
node_modules/
package-lock.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Secrets — NEVER commit. Use the *.example templates instead.
# (app-config.json holds real secrets; only app-config.example.json is committed)
app-config.json
.env
.env.local
.env.*.local
appsail/.env
appsail/.env.local
web-client/.env
web-client/.env.local
!**/.env.example

# Catalyst CLI local binding (account/project-specific; not needed in repo)
.catalystrc
.catalystrc.local

# Archived previous app (excluded from this repo)
v1/

# Local agent/editor workspace
.claude/

# Build outputs
dist/
build/
web-client/build/
*.log

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Test coverage
coverage/

# Temporary files
tmp/
temp/
*.tmp
logs/
